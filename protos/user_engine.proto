syntax = "proto3";

package user_engine;

enum Role
{
    REGULATOR = 0;
    COMMON = 1;
    READONLY = 2;
    DBA = 3;
}

message User
{
    string name = 1;
    string password = 2;
    string public_key = 3;
    Role role = 4;
}